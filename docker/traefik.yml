version: "3.8"
services:
  traefik:
    image: traefik:v3.1
    command: --api.insecure=true --providers.swarm
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock